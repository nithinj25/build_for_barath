# Local generator / training only. Lambda handlers carry their own, smaller
# requirements — never ship this file's contents to Lambda (CLAUDE.md rule 3).
numpy>=1.26
pandas>=2.1
pyarrow>=14
pyyaml>=6
scipy>=1.11
